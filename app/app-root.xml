<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
    <StackLayout>
        <Button text="Seller Login" tap="{{ navigateToSellerLogin }}" />
        <Button text="User Registration" tap="{{ navigateToUserRegistration }}" />
    </StackLayout>
</Page>